# Today Activity Card

Visualizes three Apple Health metrics for the current day:

- `kcal`
- `exercise_minutes`
- `steps`

This component follows the wristkit **five-state contract** and pairs with:

- `healthkit-handler` (ingest route)
- `healthkit-schema` (Postgres table + indexes)

